---
title: "Gettin' Crafty: Application Development articles and news"
per_page: 4
class_name: blog
full_width: true
pageable: true
---

<header>
  <div class="container">
    <div>
      <h1>Gettin' Crafty</h1>
      <h2>A Blog written by Application Craft - the Visual IDE people</h2>
    </div>
  </div>
</header>

<div class="container clearfix">

  <section class="posts">
    <% page_articles.each do |article| %>

      <article>
        <section class="meta">
          <p class="date"><%= link_to article.date.strftime('%B %e, %Y'), article %></p>

          <p class="tagged">Tagged:</p>
          <ul class="tags">
            <% article.tags.each do |tag| %>
              <li><%= tag %></li>
            <% end %>
          </ul>

          <p class="comments">
            <%= link_to 'Comments', "#{article.url}#disqus_thread", :class => "dsq-comment-count" %>
          </p>
        </section>

        <h1><%= link_to article.title, article %></h1>
        <% if article.data.image %>
          <%= image_tag article.data.image, :alt => article.title, :align => "left" %>
        <% end %>
        <p><%= article.body %></p>
      </article>

    <% end %>

    <% if paginate %>
      <section class="pagination clearfix">
        <% if next_page %>
          <p><%= link_to "&laquo; older posts", next_page %></p>
        <% end %>
        <% if prev_page %>
          <p><%= link_to "newer posts &raquo;", prev_page %></p>
        <% end %>
      </section>
    <% end %>
  </section>


  <aside>
    <section class="all-posts">
      <h4><a href="/blog/all">all posts &raquo;</a></h4>
    </section>

    <% if paginate && prev_page %>
      <section class="latest-posts">
        <h4>Latest posts...</h4>
        <ul>
          <% articles[0...8].each do |post| %>
            <li><%= link_to post.title, post %></li>
          <% end %>
        </ul>
      </section>
    <% end %>

    <section class="latest-posts">
      <h4>on this page...</h4>
      <ul>
        <% page_articles.each do |post| %>
          <li><%= link_to post.title, post %></li>
        <% end %>
      </ul>
    </section>
  </aside>

</div>


<script type="text/javascript">
/* * * CONFIGURATION VARIABLES: EDIT BEFORE PASTING INTO YOUR WEBPAGE * * */
var disqus_shortname = 'applicationcraftwebsite'; // required: replace example with your forum shortname

/* * * DON'T EDIT BELOW THIS LINE * * */
(function () {
    var s = document.createElement('script'); s.async = true;
    s.type = 'text/javascript';
    s.src = 'http://' + disqus_shortname + '.disqus.com/count.js';
    (document.getElementsByTagName('HEAD')[0] || document.getElementsByTagName('BODY')[0]).appendChild(s);
}());
</script>

<%= javascript_include_tag 'vendor/rainbow', 'vendor/rainbow/generic', 'vendor/rainbow/javascript' %>
<%= stylesheet_link_tag 'lib/rainbow/github' %>